name: tagmap_sanger
channels:
  - conda-forge
  - bioconda
dependencies:
  - python >=3.9
  - bioframe >=0.4
  - pandas >=1.5
  - numpy
  - biopython >=1.80
  - pysam >=0.21
  - pyfastx >=1.0
  # Only needed when sanger_basecaller is 'tracy'; harmless otherwise.
  - tracy >=0.7
